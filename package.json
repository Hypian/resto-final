{
  "name": "dmc-restaurant-menu",
  "version": "1.0.0",
  "description": "Optimized restaurant menu website",
  "scripts": {
    "build": "tailwindcss -i src/tailwind.css -o dist/output.css",
    "watch": "tailwindcss -i src/tailwind.css -o dist/output.css --watch",
    "build:prod": "tailwindcss -i src/tailwind.css -o dist/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  }
}